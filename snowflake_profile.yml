# example profiles.yml file
my-snowflake-db:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: https://bigdatasolutions.west-europe.azure.snowflakecomputing.com

      # User/password auth
      user: SIMO_RINTAKOSKI
      password: 

      role: SYSADMIN
      database: DEMO_WH
      warehouse: SRI_TRAINING
      schema: JAFFLE_SHOP
      threads: 4
      client_session_keep_alive: False
      query_tag: 

      # optional
      connect_retries: 0 # default 0
      connect_timeout: 10 # default: 10
      retry_on_database_errors: False # default: false 
      retry_all: False  # default: false